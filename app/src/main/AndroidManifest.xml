<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.foodverse">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".add_student_screen"></activity>
        <activity android:name=".profile_screen_setting" />
        <activity android:name=".profile_settingscreen" />
        <activity android:name=".profile_setting_screen" />
        <activity android:name=".create_food_pool" />
        <activity android:name=".navigation_bar" />
        <activity android:name=".student_pool_admin" />
        <activity android:name=".top_yellow_details_bar" />
        <activity android:name=".dashboard_screen" />
        <activity android:name=".signin_screen" />
        <activity android:name=".welcome_screen" />
        <activity android:name=".splash_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>